'use strict';

var expect = require('chai').expect;

describe('syncrhonization', function() {
  it('completes successfully if nothing changed', function() {

  });

  it('saves all new posts to a provider in data store', function() {

  });

  it('adds missing posts to a feed in data store', function() {

  });

  it('saves last post dates for each provider synchronized in a feed', function() {

  });

  it('saves last post date for each provider', function() {

  });

  it('runs initial sync properly', function() {

  });
});